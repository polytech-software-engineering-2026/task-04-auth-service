## Что сделано

<!-- краткое описание реализации -->

## Чек-лист перед отправкой на ревью

- [ ] `make check` проходит локально без ошибок
- [ ] сервис поднимается через `docker compose up` и отвечает на `/health`
- [ ] Redis реально используется для отозванных токенов (не заглушка/память процесса)
- [ ] refresh-токен одноразовый (проверено своим интеграционным тестом)
- [ ] после `/logout` и access, и refresh дают 401 (проверено своим тестом)
- [ ] есть alembic-миграции, приложение не создаёт таблицы напрямую
- [ ] слои разделены: api / service / repository
- [ ] `.env.example` актуален
- [ ] `tests/unit/` — минимум 5 тестов, `tests/integration/` покрывает весь контракт кроме register/login
- [ ] в коде нет случайно закоммиченных секретов, ключей, паролей
